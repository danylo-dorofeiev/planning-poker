{% extends 'base.html.twig' %}

{% block title %}Room Creation Form{% endblock %}

{% block body %}
    <a href="{{ path('room_list') }}">All Rooms</a>

    <h1>Room Creation Form</h1>

    {{  form_start(form) }}

    {{ form_widget(form) }}

    <br>

    <button type="submit">
        Create
    </button>

    {{ form_end(form) }}

{% endblock %}
