<button type="button" data-action="click->form#close">Close</button>

{{ form_start(form, { action: path('ticket_edit', {room_id:room.uuid, ticket_id:ticket.uuid}), method: 'POST' }) }}

{{ form_widget(form) }}

<button type="submit">Save</button>

{{ form_end(form) }}
